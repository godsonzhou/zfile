# Double Commander 操作管理器功能分析

## 概述
这个单元 `uOperationsManager` 是 Double Commander 文件管理器的核心组件之一，负责管理文件操作（如复制、移动、删除等）的执行队列和线程管理。

## 主要功能

1. **操作队列管理**：
   - 维护多个操作队列，每个队列可以包含多个文件操作
   - 支持自由队列、模态队列和单队列等不同类型的队列
   - 允许操作在不同队列间移动

2. **线程管理**：
   - 为每个文件操作创建和管理独立的执行线程
   - 处理线程终止事件并清理资源

3. **操作控制**：
   - 启动、暂停、恢复和停止文件操作
   - 调整操作在队列中的位置（移到顶部、底部或特定位置）

4. **事件通知**：
   - 提供操作添加、移除和移动的事件通知机制
   - 允许外部组件监听操作状态变化

## 核心类结构

### 1. TOperationsManagerItem
- **功能**：表示单个文件操作及其执行状态
- **关键属性**：
  - `FHandle`: 操作唯一标识符
  - `FOperation`: 文件操作对象
  - `FThread`: 执行操作的线程
  - `FQueue`: 所属队列
- **主要方法**：
  - `Start`: 启动操作执行
  - `Move`: 在队列间或队列内移动操作
  - `SetQueue`: 设置操作所属队列

### 2. TOperationsManagerQueue
- **功能**：管理一组文件操作的队列
- **关键属性**：
  - `FList`: 操作项列表
  - `FPaused`: 队列暂停状态
- **主要方法**：
  - `Insert`: 添加操作到队列
  - `Move`: 调整操作在队列中的位置
  - `Pause/UnPause`: 暂停/恢复队列执行
  - `Stop`: 停止队列中所有操作

### 3. TOperationsManager
- **功能**：全局操作管理器
- **关键功能**：
  - 创建和管理所有操作队列
  - 分配操作句柄
  - 处理线程终止事件
  - 提供事件通知机制
- **主要方法**：
  - `AddOperation`: 添加新操作到指定队列
  - `AddOperationModal`: 添加模态操作（在主线程执行）
  - `PauseAll/UnPauseAll/StopAll`: 全局控制所有操作
  - `AddEventsListener`: 添加事件监听器

## 队列类型

1. **自由队列 (FreeOperationsQueueId = 0)**：
   - 并行执行多个操作
   - 每个操作独立运行

2. **模态队列 (ModalQueueId = -1)**：
   - 在主线程执行
   - 显示模态进度窗口

3. **单队列 (SingleQueueId = 1)**：
   - 串行执行操作
   - 一次只执行一个操作

## 使用场景

1. **文件操作执行**：
   - 用户发起文件复制/移动操作时，创建操作项并添加到队列
   - 管理器自动分配线程执行

2. **操作管理**：
   - 用户可以通过界面暂停、恢复或重新排序操作
   - 管理器处理队列状态变化

3. **进度显示**：
   - 外部组件可以监听操作事件并显示进度
   - 计算所有操作的整体进度

## 技术特点

1. **线程安全**：
   - 使用原子操作分配操作句柄
   - 通过事件通知机制实现线程间通信

2. **灵活队列管理**：
   - 支持动态创建和销毁队列
   - 操作可以在队列间自由移动

3. **异常处理**：
   - 捕获并处理线程创建时的异常
   - 确保资源正确释放

## 总结

这个操作管理器为 Double Commander 提供了强大的文件操作执行框架，支持并行和串行操作执行，灵活的队列管理，以及完善的事件通知机制，是文件管理器高效稳定运行的关键组件。