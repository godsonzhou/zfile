# 文件源树构建器 (TFileSourceTreeBuilder) 功能分析

## 概述
这是一个用于构建文件系统树的类，主要用于处理文件和目录的递归遍历、符号链接处理以及基于搜索模板的过滤。它是用Object Pascal (Delphi/Lazarus)编写的。

## 主要功能

1. **文件系统树构建**：
   - 从给定节点或文件列表递归构建文件系统树
   - 跟踪文件数量、目录数量和总文件大小
   - 支持递归和非递归模式

2. **符号链接处理**：
   - 提供多种符号链接处理选项：跟随、不跟随或交互式询问用户
   - 跟踪哪些子节点包含链接

3. **搜索过滤**：
   - 支持基于搜索模板(TSearchTemplate)过滤文件和目录
   - 可以排除空目录(当使用模板时)

4. **排除功能**：
   - 可以排除根目录
   - 跟踪哪些子节点包含被排除的项目

## 核心组件

### 数据结构
- **TFileTreeNodeData**：附加到树节点的数据，包含：
  - 是否递归处理
  - 子节点是否包含链接
  - 子节点是否有被排除的项目

### 主要方法
- **BuildFromNode**：从指定节点开始构建树
- **BuildFromFiles**：从文件列表构建树
- **AddItem**：添加单个项目(文件/目录/链接)到树中
- **AddFile/AddDirectory/AddLink**：处理特定类型的项目
- **DecideOnLink**：决定如何处理符号链接

## 使用场景
这个类可能在文件管理器应用程序中使用，用于：
- 显示目录结构
- 准备文件操作(如复制、移动)
- 实现搜索功能
- 处理符号链接

## 配置选项
- **Recursive**：是否递归处理子目录
- **SymLinkOption**：符号链接处理方式
- **ExcludeRootDir**：是否排除根目录
- **ExcludeEmptyTemplateDirectories**：使用模板时是否排除空目录
- **SearchTemplate**：用于过滤文件和目录的搜索模板

## 性能考虑
- 跟踪文件数量、目录数量和总大小，避免重复计算
- 使用回调函数(AskQuestion, CheckOperationState)进行用户交互和操作状态检查

这个类提供了灵活的文件系统遍历和构建功能，特别适合需要复杂文件操作和过滤的应用程序。